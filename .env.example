# Entropy API Keys
# These are the only settings that belong in .env (secrets only).
# For provider/model config, use: entropy config set <key> <value>

# OpenAI (required if using openai as provider)
OPENAI_API_KEY=sk-...

# Anthropic — either API key OR OAuth access token
# API key (from https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=sk-ant-...
# OR OAuth access token (uncomment if using OAuth instead)
# ANTHROPIC_ACCESS_TOKEN=...
